//WRITE A FUNCTION TO REVERSE A STRING
const reverseString = (str: string) => {
//   const reverse = str.split("");
//   reverse.reverse().join("");
//   return reverse;
};

export default function Questions({}) {
  return (
    <div>
      {/* <p>{reverseString("hello i'm jae")}</p> */}
    </div>
  );
}
